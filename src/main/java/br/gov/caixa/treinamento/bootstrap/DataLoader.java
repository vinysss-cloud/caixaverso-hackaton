package br.gov.caixa.treinamento.bootstrap;

import jakarta.enterprise.context.ApplicationScoped;

/**
 * Classe mantida apenas para preservar a estrutura do projeto.
 *
 * Importante: não criamos usuário mockado/seed automático.
 * O acesso deve acontecer somente com usuário cadastrado pela tela /cadastro.
 */
@ApplicationScoped
public class DataLoader {
    // Sem carga automática de usuários.
}
